import Menu from './Menu';
import Banner from './Banner';

const Header = () => {
  return (
    <div>
      <header className='App-header'>
        <Menu />
        <Banner />
      </header>
    </div>
  );
};

export default Header;
